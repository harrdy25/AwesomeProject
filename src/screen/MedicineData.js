import { View, Text } from 'react-native'
import React from 'react'
import { normalize } from '../utils'

export default function MedicineData() {
  return (
    <View>
      <Text style={{fontSize: normalize(20)}}>MedicineData</Text>
    </View>
  )
}